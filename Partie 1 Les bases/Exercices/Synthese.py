"""
Exercice : Programme "Tout en un"

Objectif : 
    Créez un programme qui combine plusieurs notions de base en Python :
      - Déclaration et utilisation de variables et types de données
      - Structures de données (listes, tuples, dictionnaires)
      - Conditions (if, elif, else)
      - Boucles (for, while)
      - Fonctions
      - Importation et utilisation de modules

Instructions :
    1. Demandez à l'utilisateur d'entrer une série de nombres séparés par des espaces.
       - Utilisez input() pour récupérer la saisie et la méthode split() pour obtenir une liste.
       - Convertissez chaque élément en entier.
    2. Stockez ces nombres dans une liste.
    3. Créez une fonction qui, pour un nombre donné, retourne son carré.
       - Utilisez cette fonction pour créer une nouvelle liste contenant le carré de chaque nombre.
    4. Calculez et affichez la moyenne des nombres originaux.
    5. Créez un dictionnaire qui associe à chaque nombre son carré.
    6. Affichez les nombres et leurs carrés de façon formatée.
    7. Utilisez au moins une boucle for et une boucle while dans votre programme.
    8. Importez le module random et utilisez-le pour afficher un nombre aléatoire entre 1 et 100.

Liste d'aide :
    - Pour récupérer et traiter l'input : input(), str.split(), int()
    - Pour les boucles : for item in liste, while condition:
    - Pour les fonctions : def nom_fonction(param):
    - Pour les dictionnaires : dictionnaire[clé] pour accéder à une valeur
    - Pour importer un module : import module
    - Pour générer un nombre aléatoire : random.randint(1, 100)

Complétez le code ci-dessous pour réaliser l'exercice.
"""

# --- Récupérer l'input utilisateur et créer la liste de nombres ---

# --- Convertir chaque élément en entier ---

# --- Fonction pour calculer le carré ---

# --- Créer une nouvelle liste avec les carrés des nombres ---

# --- Calcul de la moyenne ---

# --- Création d'un dictionnaire associant chaque nombre à son carré ---

# --- Affichage des résultats ---

# --- Utiliser le module random pour afficher un nombre aléatoire entre 1 et 100 ---
